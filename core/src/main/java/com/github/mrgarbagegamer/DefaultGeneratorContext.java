package com.github.mrgarbagegamer;

// TODO: Delete this class
@Deprecated
final class DefaultGeneratorContext extends GeneratorContext {
    @Deprecated
    DefaultGeneratorContext(String name, int generatorId, QueueStrategy queueStrategy,
            ContextRegistry registry, SolverConfiguration config, SolverServices services) {
        super(name, generatorId, queueStrategy, registry, config, services);
    }
}
